# recorded-heroes-art-refs

《勇者名冊》送進 PixelLab 的參考圖公開托管處。

PixelLab 的生成工具只接受 https 網址（`file://` 會被拒，內嵌 base64 過長不可靠），
所以參考圖必須有一個它的伺服器抓得到的位址。這個 repo 只放這些小張參考 PNG，
不放遊戲程式、劇情或設計文件。

主專案是私有的；這裡的圖僅供生成流程使用。
